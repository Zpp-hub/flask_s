## 准备工作
- 生成requirements.txt
    - pip freeze > requirements.txt
- linux独立环境
    - python -m venv .env
    - source .env/bin/activate
    - pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

## 表结构说明
cate -> attribute -> goods


- 补充： 因为用的公用环境，所以安装包有点多